package com.stream.rdf.constant

object Uri4Rdf {
  val hostname = "http://hem.org"
    
  val homeURI = hostname + "/home"
  
  val roomURI = hostname + "/room#"
  
  val sensorURI = hostname + "/sensor#"
  
  val predURI = hostname + "/predicate#"
}